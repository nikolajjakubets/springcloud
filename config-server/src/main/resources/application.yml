spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
           uri: ${config.repo.uri:https://github.com/sq1990/springcloud}
           searchPaths: config-repo
      label: master
  profiles:
    active: dev
#动态刷新配置,guest/guest
  rabbitmq:
    host: localhost
    port: 5762 # 不是web端监控中心的15672端口
    username: guest
    password: guest
server:
  port: 9092


